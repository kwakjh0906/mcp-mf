FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 프로젝트 전체 복사
COPY . .

EXPOSE 8000

CMD ["uvicorn", "mcp.manufacturing_http_server:app", "--host", "0.0.0.0", "--port", "8000"]
